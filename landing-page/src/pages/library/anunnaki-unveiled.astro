---
import Layout from '~/layouts/PageLayout.astro';
import HeroSection from '~/components/nestedPages/AnunnakiUnveiled.astro';

const books = [0, 0, 0, 0];
const metadata = {
  title: 'Anunnaki Unveiled | Bookria',
  ignoreTitleTemplate: true,
};
const cta_url = 'https://danteseraf.gumroad.com/l/anunnakiunveiled';
---

<Layout metadata={metadata}>
  <HeroSection cta_url={cta_url} />
  
</Layout>
